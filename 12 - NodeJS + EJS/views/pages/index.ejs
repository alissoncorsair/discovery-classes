<!DOCTYPE html>


    <%- include('../partials/head', {titulo: 'Página inicial'}); %>

<body class="container">
    <%- include('../partials/header'); %>
    <main>
        <div style="  padding: 2rem 1rem;
        margin-bottom: 2rem;
        background-color: #e9ecef;
        border-radius: .3rem;">
            <h1>Node com EJS</h1>
            <p>Vamos aprender a trabalhar com arquivos EJS</p>
            <p><%= subtitle %></p>
        </div>
        Imported object from server.js

        <ul>
        <% for(var i = 0; i < object.length; i++) { %>
            <strong><li><%= object[i].title %></strong> <%= object[i].content %></li>
          <% } %>
        </ul>
    </main>
    <%- include('../partials/footer'); %>
</body>

</html>